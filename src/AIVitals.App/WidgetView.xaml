<UserControl x:Class="AIVitals.App.WidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AIVitals.App"
             Background="Transparent"
             Foreground="{DynamicResource TextBrush}"
             FontFamily="Segoe UI Variable Text, Segoe UI"
             UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Border Margin="7" Padding="11,8,11,10" CornerRadius="15"
            Background="{DynamicResource WidgetGlassBrush}"
            BorderBrush="{DynamicResource LineBrush}" BorderThickness="1">
        <Border.Effect><DropShadowEffect Color="#000000" BlurRadius="17" ShadowDepth="4" Opacity="0.32" /></Border.Effect>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="19" /><RowDefinition Height="4" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="6" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <local:ActivityRingsIcon Width="18" Height="18" OuterBrush="{DynamicResource SignalBrush}"
                                         MiddleBrush="{DynamicResource CodexBrush}" InnerBrush="{DynamicResource WarmBrush}"
                                         TrackBrush="{DynamicResource LineBrush}" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="AI Vitals" FontSize="10" FontWeight="SemiBold" Visibility="{Binding FullHeaderVisibility}" />
                <TextBlock Grid.Column="3" Style="{StaticResource FluentIcon}" VerticalAlignment="Center"
                           Text="{Binding InteractionGlyph}" FontSize="12" Foreground="{DynamicResource MutedTextBrush}"
                           ToolTip="{Binding InteractionText}" AutomationProperties.Name="{Binding InteractionText}" />
            </Grid>

            <ItemsControl Grid.Row="2" ItemsSource="{Binding Connections}" Visibility="{Binding RingsVisibility}">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="116" Height="116" Margin="1" ToolTip="{Binding DetailText}" AutomationProperties.Name="{Binding AccessibleName}">
                            <Grid.RowDefinitions><RowDefinition Height="18" /><RowDefinition Height="76" /><RowDefinition Height="22" /></Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <local:SvgLogo Width="14" Height="14" Source="{Binding LogoSource}" Tint="{Binding LogoBrush}" />
                                <TextBlock Margin="5,0,0,0" VerticalAlignment="Center" Text="{Binding DisplayName}" FontSize="10" FontWeight="SemiBold" />
                            </StackPanel>
                            <Grid Grid.Row="1" Width="76" Height="76" HorizontalAlignment="Center">
                                <ItemsControl ItemsSource="{Binding RingBands}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><Grid /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate><DataTemplate><local:UsageRing Width="{Binding RingDiameter}" Height="{Binding RingDiameter}" Value="{Binding UsageValue}" SignalBrush="{Binding SignalBrush}" TrackBrush="{DynamicResource LineBrush}" /></DataTemplate></ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding PrimaryUsageText}"
                                           FontFamily="Cascadia Mono, Consolas" FontSize="13" FontWeight="Bold" />
                            </Grid>
                            <ItemsControl Grid.Row="2" ItemsSource="{Binding Bands}" HorizontalAlignment="Center">
                                <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel HorizontalAlignment="Center" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="3,0">
                                            <TextBlock Text="{Binding ShortLabel}" FontFamily="Cascadia Mono, Consolas" FontSize="9" Foreground="{Binding SignalBrush}" />
                                            <TextBlock Margin="2,0,0,0" Text="{Binding UsageText}" FontFamily="Cascadia Mono, Consolas" FontSize="9" FontWeight="SemiBold" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl Grid.Row="2" ItemsSource="{Binding Connections}" Visibility="{Binding HorizontalVisibility}" VerticalAlignment="Center">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2" ToolTip="{Binding DetailText}" AutomationProperties.Name="{Binding AccessibleName}">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="84" /><ColumnDefinition Width="8" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <local:SvgLogo Width="15" Height="15" Source="{Binding LogoSource}" Tint="{Binding LogoBrush}" />
                                <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="{Binding DisplayName}" FontSize="10" FontWeight="SemiBold" />
                            </StackPanel>
                            <ItemsControl Grid.Column="2" ItemsSource="{Binding Bands}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="17">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="27" /><ColumnDefinition Width="*" /><ColumnDefinition Width="38" /></Grid.ColumnDefinitions>
                                            <TextBlock VerticalAlignment="Center" Text="{Binding ShortLabel}" FontFamily="Cascadia Mono, Consolas" FontSize="9" Foreground="{Binding SignalBrush}" />
                                            <local:UsageMeter Grid.Column="1" Height="7" VerticalAlignment="Center" Value="{Binding UsageValue}" SignalBrush="{Binding SignalBrush}" TrackBrush="{DynamicResource LineBrush}" />
                                            <TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Text="{Binding UsageText}" FontFamily="Cascadia Mono, Consolas" FontSize="10" FontWeight="Bold" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl Grid.Row="2" ItemsSource="{Binding Connections}" Visibility="{Binding VerticalVisibility}" HorizontalAlignment="Center" VerticalAlignment="Center">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="{Binding VerticalWidth}" Margin="3,0" ToolTip="{Binding DetailText}" AutomationProperties.Name="{Binding AccessibleName}">
                            <local:SvgLogo Width="18" Height="18" Margin="0,0,0,6" HorizontalAlignment="Center"
                                           Source="{Binding LogoSource}" Tint="{Binding LogoBrush}" />
                            <ItemsControl ItemsSource="{Binding Bands}" HorizontalAlignment="Center">
                                <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="27" Height="340">
                                            <Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="13" /><RowDefinition Height="15" /></Grid.RowDefinitions>
                                            <local:UsageMeter Width="9" Margin="0,1,0,4" HorizontalAlignment="Center" Orientation="Vertical" Value="{Binding UsageValue}" SignalBrush="{Binding SignalBrush}" TrackBrush="{DynamicResource LineBrush}" />
                                            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Text="{Binding ShortLabel}" FontFamily="Cascadia Mono, Consolas" FontSize="8" Foreground="{Binding SignalBrush}" />
                                            <TextBlock Grid.Row="2" HorizontalAlignment="Center" Text="{Binding UsageText}" FontFamily="Cascadia Mono, Consolas" FontSize="9" FontWeight="Bold" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
